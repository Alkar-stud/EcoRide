RewriteEngine On

# Si le fichier ou dossier existe, on laisse Apache le servir normalement
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Sinon, on redirige toutes les autres requêtes vers index.html
RewriteRule ^ index.html [L]